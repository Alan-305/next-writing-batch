/**
 * nexus_project/services/ocr.py と同じ指示（問題文取り込み用）。
 * [cite: …] 等は含めない。
 */

export const PROBLEM_PLAIN_PROMPT =
  "この画像やドキュメントに印刷または手書きされている、英作文の問題文・指示・条件を" +
  "正確にテキストに書き出してください。日本語と英語の両方がある場合はそのまま残してください。" +
  "余計な解説や挨拶は書かないでください。";

/** extract_problem_structured の intro と同一 */
export const PROBLEM_STRUCTURED_INTRO = `あなたは教材のテキスト化を手伝うアシスタントです。以下の画像（複数ある場合はページ順）を読み、次のMarkdown形式「のみ」を出力してください。前置き・後書き・自己評価は禁止です。

## 問題文・指示
（英語・日本語を含め、指示・設問番号・字数制限を忠実に転記する）

## 図表・グラフの数値（読み取れる範囲で）
- グラフや表が無い場合は次の1行だけ書く：（該当なし）
- ある場合は Markdown の表で示す。列は「年」「項目名」「数値」など読み取れるヘッダを付ける。数値は画像に見えるとおりに。読み取れないセルは空欄または「?」。

## 出典・注記（あれば）
（画像内にあれば転記。なければ次の1行だけ：（なし））
`;
